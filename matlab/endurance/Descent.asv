function [outDescent] = Descent(plane,altitude)
%UNTITLED26 Summary of this function goes here
%   Detailed explanation goes here

alpha_glide = calcAlphaGliding(plane);
CL = calcCL(plane,alpha_glide);
CD = calcCd(plane,alpha_glide);
vs = ((2*plane.m*9.81/plane.test.rho*plane.S)^0.5)*(CD/(CL^1.5));

glide_path_angle = 
descent.time = altitude/vs;




outDescent = descent;
end

